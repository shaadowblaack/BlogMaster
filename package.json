{
  "name": "blog-master",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "sh -c 'cd client && bun install && bun run build && cd ../server && bun install && bun run build'",
    "start": "sh -c 'cd server && bun start'"
  }
}
